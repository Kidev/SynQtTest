{
  "_about": [
    "Every attack SynQt claims to defend, and the test that proves it still fails.",
    "",
    "This is an index, not a suite. Each of these tests lives beside the code it is about,",
    "because that is where somebody changing that code will run it; what this file adds is",
    "the other half, which no scattered set of tests can give on its own: a list of the",
    "attack surface that can be read in one sitting, and a guard that fails when an entry",
    "names a test that is no longer there. tools/synqt/tests/test_security_index.py is that",
    "guard, and it runs in the ordinary pytest job on all three platforms.",
    "",
    "The rule for adding one: a defect found by review or by report gets a test that fails",
    "without the fix, and an entry here. Proving the test catches the bug is done by reverting",
    "the fix and watching it go red; the entry is what stops that proof from being a thing",
    "somebody did once."
  ],
  "attacks": [
    {
      "id": "delegated-answers-nobody-is-waiting-for",
      "what": "An edge running identity on an auth entity keeps every answer that entity sends, keyed by request id, and reads one only while a route handler is still waiting for it. An answer arriving after its twenty-second deadline is therefore kept forever, and so is one naming a request id the edge never issued. The login and callback routes are open, so making the auth entity slow is enough to grow the table on demand; an auth entity that has been compromised can grow it as fast as it can write.",
      "defended_by": "An answer is kept only while somebody is waiting for it, and a waiter takes its request id out of the awaited set before it returns, so an answer that arrives late is dropped rather than filed.",
      "file": "tests/memory/tst_memory.cpp",
      "test": "anEdgeKeepsNoAnswerNobodyIsWaitingFor"
    },
    {
      "id": "one-console-question-asks-for-the-whole-table",
      "what": "A console asks the monitor for its history with a row limit of its own choosing. The limit crosses as a plain integer -- the contract vocabulary sizes strings and lists and says nothing about integers -- and every row it asks for is built into a list in memory and serialized back over the link, so one question could make the monitor materialize its whole table at once.",
      "defended_by": "The store clamps the limit to a range it decides, so the largest answer it can be made to produce is a property of the store rather than of the number it was sent. Every other stage of this pipeline is already bounded; this was the one that was not.",
      "file": "tests/monitor/tst_store.cpp",
      "test": "oneQuestionCannotAskForTheWholeTable"
    },
    {
      "id": "an-event-recorded-outside-every-filter",
      "what": "Severity and category cross the ingest link as their numbers. An entity sending a number this build has no enumerator for gets an event that is written to the history and then matches no category filter and no severity floor, so it is in the record and cannot be found in it. An operator reading a quiet period cannot tell it from a hidden one.",
      "defended_by": "Both fields are checked against the vocabulary where the record arrives, and a number outside it is read as the ordinary value rather than kept as an unfilterable one. That is also what keeps the two enums inside the array the hot-path level check indexes.",
      "file": "tests/monitor/tst_pipeline.cpp",
      "test": "aNumberOutsideTheVocabularyIsReadAsTheOrdinaryValue"
    },
    {
      "id": "nested-callbacks-exhaust-the-stack",
      "what": "Callbacks are fired at the open /auth/callback route faster than the token exchanges behind them finish. Each one waits in a nested event loop that goes on serving requests, so every arrival nests inside the last and the depth follows the request rate until the stack runs out.",
      "defended_by": "At most sixty-four callbacks are exchanged at once, whether identity runs on the edge or on an auth entity, and the nesting may spend at most a quarter of the running thread's stack, whichever comes first. The ceiling used to cover the auth-entity path alone, which is the one an attacker is least likely to be talking to; and a count on its own turned out to be a guess at what a stack holds, since sixty-four levels fit the eight megabytes Linux and macOS give the main thread and not the megabyte Windows gives it.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "concurrentCallbacksAreBoundedWithIdentityInProcess"
    },
    {
      "id": "id-token-key-chosen-by-position",
      "what": "An ID token that names no signing key is verified against whichever key the provider happened to list first. During a rotation a provider publishes two, so whether a token is accepted depends on the order of a JSON array rather than on who signed it.",
      "defended_by": "A token with no kid is accepted only where the provider publishes exactly one key. With more than one it is refused, and the refusal names the ambiguity rather than the signature.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "anIdTokenThatNamesNoKeyIsRefusedWhenTheProviderPublishesTwo"
    },
    {
      "id": "mesh-key-readable-while-it-is-written",
      "what": "Another account on the build machine reads the project CA private key in the window between openssl creating the file and the tool restricting it. Whoever holds that key issues themselves a certificate for any entity, and every mesh link accepts it.",
      "defended_by": "The key file is created 0600 before openssl is run, so it is never readable by anyone else, rather than made unreadable a moment after it was.",
      "file": "tools/synqt/tests/test_m10.py",
      "test": "test_a_private_key_is_never_readable_by_anyone_else_even_briefly"
    },
    {
      "id": "entity-name-escapes-the-mesh-directory",
      "what": "A name given to `synqt mesh cert` is put literally into the key and certificate file names, the certificate subject and the SAN. A separator writes a private key outside the mesh directory; a `/` opens a second RDN, so the identity a peer is authenticated as is not the one the operator asked for.",
      "defended_by": "The same name rule `synqt check` holds a declared entity to is applied to the name typed at the prompt, before openssl is reached.",
      "file": "tools/synqt/tests/test_m10.py",
      "test": "test_a_name_that_is_not_an_entity_name_never_reaches_openssl"
    },
    {
      "id": "cross-site-websocket-hijack",
      "what": "A page on another origin opens a WebSocket to the edge, and the browser attaches the victim's session cookie to it.",
      "defended_by": "The upgrade verifier compares Origin against security.allowed_origins before a socket exists. A browser cannot forge Origin.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "disallowedOriginRejectedBeforeSocket"
    },
    {
      "id": "origin-gate-refuses-everyone",
      "what": "The origin gate is set against an origin no browser can arrive with, so it refuses every visitor and the app loops at the sign-in screen. It shipped that way: an edge built its own origin out of its bind address, and the default bind is 0.0.0.0.",
      "defended_by": "The edge derives its origin from public.origin, and a wildcard bind resolves to localhost rather than to itself. The test arrives the way a browser has to, at an address the edge did not name.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "wildcardBoundEdgeAcceptsTheBrowserThatCanReachIt"
    },
    {
      "id": "forged-session-at-upgrade",
      "what": "A hand-written client presents a session cookie it made up, or none at all.",
      "defended_by": "The verifier looks the credential up in the session manager and denies 401 before a socket exists.",
      "file": "tests/m7-caller/tst_m7.cpp",
      "test": "forgedSessionRefusedAtUpgrade"
    },
    {
      "id": "read-access-outlives-the-credential",
      "what": "A visitor signs out, and the tab that was open keeps receiving everything the owner pushes to the connect points that session had acquired.",
      "defended_by": "Ending a session closes the connections it authorized. Which points a connection hosts is decided once, at the upgrade, so revoking the credential has to take the socket with it.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "revokingASessionClosesTheConnectionsItAuthorized"
    },
    {
      "id": "read-access-outlives-the-credential-on-a-threaded-edge",
      "what": "The same sign-out, on an edge running `threads: N`, where the socket to close belongs to an IO thread and not to the one the session manager runs on.",
      "defended_by": "The edge ends a connection through its device, which is on its own thread whichever thread the socket is on, and never by calling close() on the socket across a thread, which does nothing and reports nothing.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "revokingASessionClosesAThreadedConnection"
    },
    {
      "id": "read-access-outlives-an-elevated-credential",
      "what": "The same sign-out, on a session that was elevated while it was connected. An elevation rotates the credential, and the edge went on holding the socket under the id the handshake presented, so ending the session looked for the current one, found nothing, and closed nothing. The visitor's calls failed from then on, and the tab went on receiving every push.",
      "defended_by": "The edge follows the rotation in its own bookkeeping: the socket and the session's Sources move to the new credential, so the three ways a session ends still reach them.",
      "file": "tests/m6-client/tst_m6.cpp",
      "test": "revokingAnElevatedSessionClosesTheConnectionItAuthorized"
    },
    {
      "id": "sign-out-that-signs-nobody-out",
      "what": "Session.logout() resets the client and leaves the session alive at the edge, so the next page load signs the visitor straight back in.",
      "defended_by": "SynClient answers the request by reaching the edge's logout route; the session is gone from the edge afterwards.",
      "file": "tests/m6-client/tst_m6.cpp",
      "test": "logoutEndsTheSessionAtTheEdgeAndNotOnlyInTheClient"
    },
    {
      "id": "anonymous-where-identity-is-required",
      "what": "A caller with no identity connects to an edge whose project requires one.",
      "defended_by": "The verifier refuses it at the upgrade, before any object exists.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "anonymousUpgradeRefusedWhenIdentityIsRequired"
    },
    {
      "id": "under-scoped-caller",
      "what": "A signed-in user calls a slot their scope does not permit, or asks for another user's rows.",
      "defended_by": "The owner authorizes Caller inside every privileged slot; a consumer-side check is never the boundary.",
      "file": "tests/m7-caller/tst_m7.cpp",
      "test": "userAuthorizationMatrix"
    },
    {
      "id": "rogue-entity-no-certificate",
      "what": "A process on the network dials a mesh listener with no client certificate.",
      "defended_by": "Both ends verify the peer against the project CA; the handshake fails before any connection exists.",
      "file": "tests/m3-mesh/tst_m3.cpp",
      "test": "missingCertificateRejected"
    },
    {
      "id": "rogue-entity-foreign-certificate",
      "what": "The same, with a certificate from some other authority.",
      "defended_by": "The CA is the project's own and is the only one either end trusts.",
      "file": "tests/m3-mesh/tst_m3.cpp",
      "test": "foreignCertificateRejected"
    },
    {
      "id": "entity-not-on-the-consumer-list",
      "what": "An entity holding a valid project certificate acquires a connect point it was never listed as a consumer of.",
      "defended_by": "Deny by default: the owner refuses a connection from an entity the point does not name, certificate or no certificate.",
      "file": "tests/m7-caller/tst_m7.cpp",
      "test": "unlistedEntityRefusedAtHandshake"
    },
    {
      "id": "browser-reaching-past-the-edge",
      "what": "Anything but the web edge calls the database's connect point.",
      "defended_by": "The database authorizes the calling entity by its verified certificate subject inside the slot.",
      "file": "tests/m7-caller/tst_m7.cpp",
      "test": "databaseRefusesNonEdgeEntity"
    },
    {
      "id": "consumer-writes-the-owners-model",
      "what": "A consumer calls setData on a replicated model and the owner's rows change under it.",
      "defended_by": "SynQt::SourceModel refuses a write that arrived from a replica; a consumer that wants owner state changed calls a slot.",
      "file": "tests/m1-contract/tst_modelwrite.cpp",
      "test": "consumerCannotWriteTheModel"
    },
    {
      "id": "consumer-writes-its-own-replica-cache",
      "what": "A consumer calls setData on the model its facade handed it. The write never reaches the owner, but QtRO's model Replica takes it into its own cache and answers true, so that consumer goes on showing a value nobody else has.",
      "defended_by": "The facade hands out SynQt::ReadOnlyModel over the Replica's model, never the Replica's own: setData returns false and flags() clears ItemIsEditable, so a delegate offers no editor and a direct caller is refused. The owner-side half is SynQt::SourceModel.",
      "file": "tests/consumer-facade/tst_consumer.cpp",
      "test": "facadeSurfacesAndErgonomics"
    },
    {
      "id": "undeclared-field-crosses-the-wire",
      "what": "A row carries a field the contract never declared (an owner id, an email) and it reaches the browser.",
      "defended_by": "The boundary keeps the declared roles and drops the rest, where the rows are published.",
      "file": "tests/m1-contract/tst_modelwrite.cpp",
      "test": "anUndeclaredFieldIsStillDropped"
    },
    {
      "id": "oversized-value-past-a-declared-width",
      "what": "A value longer than the contract's declared width is published or passed, and the receiver stores whatever arrives.",
      "defended_by": "A declared width is a rule the owner keeps at the boundary: the publish is refused whole rather than truncated into something that looks right.",
      "file": "tests/m1-contract/tst_modelwrite.cpp",
      "test": "aRoleOverItsBoundRefusesThePublish"
    },
    {
      "id": "signal-argument-past-a-declared-width",
      "what": "The same, on the way out, where nothing on the receiving side would ever see the original.",
      "defended_by": "The signal is not raised at all.",
      "file": "tests/m1-contract/tst_modelwrite.cpp",
      "test": "aSignalArgumentOverItsBoundIsNeverRaised"
    },
    {
      "id": "sql-injection-through-a-parameter",
      "what": "A caller-supplied value carries SQL and the statement runs it.",
      "defended_by": "Every value is bound, never concatenated; the payload is stored and read back as the text it is.",
      "file": "tests/m9-providers/tst_m9.cpp",
      "test": "parameterizedValuesAreInjectionInert"
    },
    {
      "id": "outbound-allowlist-escaped-by-spelling",
      "what": "A URL that is not the declared place but begins with its text (userinfo, a host suffix, a path suffix) is called, and the endpoint's declared headers, which is where the API key lives, go with it.",
      "defended_by": "The allowlist compares scheme, host, port and path segments, and refuses userinfo outright.",
      "file": "tests/m9-providers/tst_m9.cpp",
      "test": "theOutboundAllowlistIsAPlaceAndNotAStringPrefix"
    },
    {
      "id": "plaintext-to-an-external-engine",
      "what": "A release build reaches a database or an upstream API over an unverified plaintext connection.",
      "defended_by": "Refused in release, on both the provider and the outbound HTTP path.",
      "file": "tests/m9-providers/tst_m9.cpp",
      "test": "postgresRefusesPlaintextInRelease"
    },
    {
      "id": "login-csrf",
      "what": "An attacker hands a victim a valid state and their own authorization code, so the victim ends up signed in as the attacker.",
      "defended_by": "The callback must carry the state cookie the login set in that same browser, compared in constant time.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "loginCsrfRejected"
    },
    {
      "id": "replayed-or-invented-oauth-state",
      "what": "A callback arrives with a state this edge never issued, or one it has already consumed.",
      "defended_by": "Only a pending state this edge is still holding is accepted, and taking it consumes it.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "unknownStateRejected"
    },
    {
      "id": "id-token-from-the-wrong-issuer",
      "what": "A token signed by somebody else, or for somebody else, is presented as proof of identity.",
      "defended_by": "The ID token's signature is verified against the provider's JWKS and its claims against the provider that was asked.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "oidcWrongIssuerRejected"
    },
    {
      "id": "dev-stub-identity-in-a-real-build",
      "what": "The development sign-in ships. It authenticates whoever asks as one of the people the project configured, with no password and no third party involved, so an edge that ran it in production would hand a session to anyone who found the login route.",
      "defended_by": "Three gates, and each closes on its own. The server is started only by an edge given --dev, which `synqt dev` passes and no build, `synqt serve`, unit or container ever does. StubIdentityServer refuses to be constructed without an acknowledgement that can only be written on purpose. And the runtime refuses the devStub provider entry itself without the same flag, so an edge that somehow held the server still answers the login route with 403.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "devStubRefusedWithoutGate"
    },
    {
      "id": "unkeyed-caller-on-an-inbound-api",
      "what": "A caller reaches an entity's inbound API surface with no key, or with the wrong one.",
      "defended_by": "The key is compared in constant time before the handler runs at all.",
      "file": "tests/api-inbound/tst_apiinbound.cpp",
      "test": "noKeyAndAWrongKeyAreBothRefusedBeforeTheHandler"
    },
    {
      "id": "oversized-frame",
      "what": "A peer sends a frame large enough to be worth buffering, repeatedly.",
      "defended_by": "The frame cap rejects before buffering, and the read-buffer cap closes a connection whose peer outruns the reader.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "oversizedFrameRejected"
    },
    {
      "id": "connection-flood",
      "what": "One address opens connections until the edge runs out of something.",
      "defended_by": "Per-IP and global caps, applied inside the verifier.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "connectionCapRefusesTheOneOverTheLimit"
    },
    {
      "id": "oversized-request-body",
      "what": "A stranger posts a huge body to the edge to make it buffer megabytes per request.",
      "defended_by": "QHttpServer's body ceiling is set from what the entity accepts (64 KiB with no network.inbound), so the body is refused with 413 before a route sees it.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "anOversizedBodyIsRefusedBeforeARouteSeesIt"
    },
    {
      "id": "request-flood",
      "what": "One address sends requests as fast as it can, to spend the edge's time rather than its connections.",
      "defended_by": "security.max_requests_per_second, off by default and answered with 429; synqt check refuses it on an edge that names a balancer, where Qt would count every visitor as one peer.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "aFloodOfRequestsFromOneAddressIsRefused"
    },
    {
      "id": "half-open-handshake",
      "what": "A peer connects and never finishes the upgrade, holding a socket for free.",
      "defended_by": "A timer per accepted socket closes one that has sent nothing by security.handshake_timeout_ms.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "stalledUpgradeClosed"
    },
    {
      "id": "design-editor-without-the-token",
      "what": "Anything on the machine reaches the local design server's API and writes to the project.",
      "defended_by": "Every /api request carries a token minted for that run, compared in constant time.",
      "file": "tools/synqt/tests/test_design_server.py",
      "test": "test_no_token_is_refused"
    },
    {
      "id": "design-editor-dns-rebinding",
      "what": "A page the visitor is looking at resolves a name to 127.0.0.1 and drives the local design server as its own origin.",
      "defended_by": "The Host header has to be this server, and a request that says where it came from has to say this editor.",
      "file": "tools/synqt/tests/test_design_server.py",
      "test": "test_a_host_header_that_is_not_this_server_is_refused"
    },
    {
      "id": "under-scoped-caller-reads-a-gated-member",
      "what": "A signed-in visitor without the scope a member is gated on reads it anyway: the slot may refuse, but a prop and a model are pushed state and take no call to read.",
      "defended_by": "A `<scope>` gate is on the flow, not the shape. The Source answering that caller never seeds or follows a member they may not see, so no value is ever serialized toward them, and losing the scope withdraws what was already sent.",
      "file": "tests/m7-caller/tst_memberscope.cpp",
      "test": "aMirrorPublishesOnlyWhatItsCallerMaySee"
    },
    {
      "id": "a-caller-reaches-the-tier-that-is-not-theirs",
      "what": "A signed-in visitor reaches the entity behind a front that serves a scope they do not hold, or an unauthorized caller reaches any of them.",
      "defended_by": "A front hands each caller to the entity its scope names and to no other, so no link to the rest is opened for them at all; the front's own member gate refuses a slot before anything is forwarded, so the entity behind never sees a call its caller had no scope for.",
      "file": "tests/m7-caller/tst_front.cpp",
      "test": "anUnderScopedCallerIsRefusedAtTheFront"
    },
    {
      "id": "an-entity-off-the-consumer-list-reaches-the-books",
      "what": "A genuine mesh entity that is not on a connect point's consumer list acquires it anyway.",
      "defended_by": "The consumer list is the whole of the rule: the owner refuses the connection at the point, so the Replica never becomes valid and the owner's QML needs no check of its own.",
      "file": "tests/fix1-auction/tst_fix1.cpp",
      "test": "theBooksRefuseAnEntityThatIsNotTheEdge"
    },
    {
      "id": "a-signed-out-caller-calls-a-gated-slot",
      "what": "A browser console calls a slot the caller's scope does not reach, as in placeBid while signed out.",
      "defended_by": "The `<scope>` gate on the member is applied by the owner before the slot runs, so there is no check in the QML to forget.",
      "file": "tests/fix1-auction/tst_fix1.cpp",
      "test": "auctionAuthorizationMatrix"
    },
    {
      "id": "desktop-login-open-redirect",
      "what": "A visitor is sent a login link whose `return` points somewhere the attacker controls, so the finished sign-in is redirected to them.",
      "defended_by": "The edge accepts one shape of return URL and no other: http, a loopback literal, a port, no userinfo, no path, no query, no fragment. Anything else refuses the login outright, before the provider is contacted.",
      "file": "tests/m8-auth/tst_desktop.cpp",
      "test": "returnMustBeLoopback"
    },
    {
      "id": "desktop-claim-read-out-of-browser-history",
      "what": "Somebody reads the loopback URL back out of the system browser's history, or off a synced copy of it, and uses what it carried to sign in as that visitor.",
      "defended_by": "The URL carries a claim code, never the session. The code lives a minute, is spent by the first attempt whether or not that attempt was right, and buys nothing without the S256 verifier that never left the app.",
      "file": "tests/m8-auth/tst_desktop.cpp",
      "test": "claimIsSingleUseAndBoundToItsVerifier"
    },
    {
      "id": "desktop-login-answered-by-a-local-process",
      "what": "A process on the same machine connects to the app's loopback port and hands it a claim code for an account the attacker controls, signing the visitor in as somebody else.",
      "defended_by": "The client refuses any arrival that does not carry the nonce it generated for the sign-in it started, compared in constant time, and claims nothing.",
      "file": "tests/m8-auth/tst_desktop.cpp",
      "test": "loopbackReceiverServesOneAnswer"
    },
    {
      "id": "desktop-login-on-a-project-without-one",
      "what": "A loopback login is started against an edge whose project ships no desktop client, so the redirect goes to a port only something hostile would be listening on.",
      "defended_by": "The whole flow is off unless the project builds a desktop client: the login refuses the return, and the claim endpoint answers nothing.",
      "file": "tests/m8-auth/tst_desktop.cpp",
      "test": "withoutADesktopClientThereIsNoDesktopLogin"
    },
    {
      "id": "claim-endpoint-reached-from-page-script",
      "what": "Script on any page posts to the claim endpoint, spending a code a sign-in is halfway through.",
      "defended_by": "The endpoint refuses anything carrying an Origin at all, which puts it out of reach of page script rather than relying on the code being unguessable.",
      "file": "tests/m8-auth/tst_desktop.cpp",
      "test": "claimRefusesAnythingWithAnOrigin"
    },
    {
      "id": "stolen-device-credential-replayed",
      "what": "A device credential is copied off a disk and redeemed from another machine, alongside the machine it was taken from.",
      "defended_by": "Every redemption rotates, so the two copies diverge at the next relaunch. Presenting a retired generation past the overlap window revokes the family and every session it opened, which is what turns a silent theft into an event the edge acts on.",
      "file": "tests/m8-auth/tst_device.cpp",
      "test": "aRetiredGenerationPastTheWindowRevokesTheFamily"
    },
    {
      "id": "device-credential-used-as-a-session",
      "what": "A device credential found on a disk is presented as the session cookie at the WebSocket upgrade.",
      "defended_by": "What is persisted is not a session. Neither half of it is a live session credential, so the upgrade verifier refuses it before a socket exists; it is only redeemable at one POST route, once.",
      "file": "tests/m8-auth/tst_device.cpp",
      "test": "aDeviceSecretIsRefusedAtTheUpgrade"
    },
    {
      "id": "device-redemption-from-page-script",
      "what": "Page script on any origin POSTs to the device route to turn a credential into a session in a browser.",
      "defended_by": "The route refuses any request carrying an Origin header at all, which puts it out of reach of page script rather than relying on the credential staying secret.",
      "file": "tests/m8-auth/tst_device.cpp",
      "test": "anOriginHeaderIsRefused"
    },
    {
      "id": "signed-out-device-still-redeemable",
      "what": "A visitor signs out, and the credential their app left on disk still buys a session.",
      "defended_by": "Signing out deletes the family the session came from, read from what the edge recorded when it minted that session rather than from anything the caller sent.",
      "file": "tests/m8-auth/tst_device.cpp",
      "test": "logoutEndsTheFamily"
    },
    {
      "id": "stale-scope-on-a-redeemed-session",
      "what": "A visitor demoted since they signed in keeps the scope they had for the rest of their device credential's month.",
      "defended_by": "Redemption re-runs the scope mapping hook against the identity stored with the family, every time, instead of carrying a scope forward.",
      "file": "tests/m8-auth/tst_device.cpp",
      "test": "redemptionRederivesScope"
    },
    {
      "id": "device-family-denial-of-service",
      "what": "Somebody who learned a device family id offers wrong secrets for it to get its owner signed out.",
      "defended_by": "Only a retired generation past the overlap window revokes a family. A secret belonging to no generation is refused and costs the owner nothing, and the route is rate limited per address.",
      "file": "tests/m8-auth/tst_device.cpp",
      "test": "anUnknownCredentialIsRefused"
    },
    {
      "id": "reuse-detection-fired-by-a-broken-store",
      "what": "A machine whose secure store cannot complete a write signs its owner out: the rotation it could not save leaves the retired generation on disk, and the next launch presents it as a second copy in circulation.",
      "defended_by": "A write that fails, and a write abandoned at its deadline, both leave nothing behind, so the retired generation is never presented again and the family survives. Revocation stays the answer to two copies existing, which is what makes it worth acting on.",
      "file": "tests/m8-auth/tst_devicestore.cpp",
      "test": "aRotationThatCannotBeStoredLeavesNothingBehind"
    },
    {
      "id": "device-rate-window-spent-by-a-neighbour",
      "what": "Somebody behind the same address spends the device route's per-address rate window, and every honest client on that address deletes the stored sign-in it is holding, because the refusal it gets back is the one that means the credential is dead.",
      "defended_by": "The rate window answers 429 with Retry-After and not the credential refusal, which gives nothing away because it is decided before the credential is read, and the client drops what is stored only on the edge's own refusal of the credential. Everything else, a rate limit and a proxy's bad minute alike, is waited out with the credential intact.",
      "file": "tests/m8-auth/tst_devicestore.cpp",
      "test": "aRateLimitDoesNotCostTheStoredSignIn"
    },
    {
      "id": "device-credential-burnt-by-a-refused-socket",
      "what": "An edge that answers HTTP but refuses this client's WebSocket leaves it reconnecting every few hundred milliseconds, and each reconnect spends the stored credential again for a session identical to the one already in hand: a generation retired per reconnect, the shared rate window gone within the minute, and then the credential deleted over the refusal that follows.",
      "defended_by": "The credential buys a session, not a connection. It is spent at most once per session that a connection has been accepted for, so a socket the session cannot answer for is retried with the session it already bought.",
      "file": "tests/m8-auth/tst_devicestore.cpp",
      "test": "aClientThatCannotConnectSpendsTheCredentialOnce"
    },
    {
      "id": "device-credential-copied-off-disk",
      "what": "Somebody with read access to a machine's files copies the stored sign-in and replays it from their own, which a credential written to a file would let them do without ever facing the OS store that was supposed to protect it.",
      "defended_by": "There is no file fallback anywhere, on any platform or in any build: a machine with no secure store keeps the credential for the life of the process and signs in again next launch. Proved against the real config, data and cache directories rather than against the absence of a code path, with the store injected so the claim is checked on every platform and not only where a keyring happens to be missing.",
      "file": "tests/m8-auth/tst_devicestore.cpp",
      "test": "withoutAStoreNothingIsWritten"
    },
    {
      "id": "forged-forwarded-for-escapes-the-cap",
      "what": "A visitor writes their own X-Forwarded-For to be counted as somebody else, so the per-IP connection cap becomes a bucket they pick per connection.",
      "defended_by": "The forwarding header is read only when the direct peer is on public.trusted_proxies; from anyone else it is ignored outright and the peer address is the visitor.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "aForgedForwardedHeaderDoesNotMoveTheCap"
    },
    {
      "id": "prepended-forwarded-hops",
      "what": "A visitor behind the real balancer prepends hops to X-Forwarded-For, so the address the edge reads is one they chose rather than the one the balancer observed.",
      "defended_by": "Only the rightmost entry that is not itself a trusted hop is taken as the client; everything left of it was written by the client and is never the answer.",
      "file": "tests/m5-webedge/tst_clientaddress.cpp",
      "test": "appendedHopsDoNotBecomeTheClient"
    },
    {
      "id": "login-csrf-across-replicas",
      "what": "An attacker begins a login, hands the victim the state plus their own authorization code, and aims the callback at an edge process that never saw the login start.",
      "defended_by": "The browser CSRF binding is held with the state by the identity engine, not by the edge that began the login, and is compared there in constant time before the code is spent.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "aCallbackWithTheWrongBindingIsRefusedOnEveryEdge"
    },
    {
      "id": "callback-replayed-against-another-replica",
      "what": "A completed OAuth callback is replayed against a second edge process, to buy a second session from a record the first process already spent.",
      "defended_by": "The pending record is consumed on the identity engine, so it is single-use for the whole deployment rather than once per process.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "aReplayedCallbackIsRefusedOnEveryEdge"
    },
    {
      "id": "desktop-claim-replayed-against-another-replica",
      "what": "A desktop claim code, read out of the system browser's history, is spent again against a second edge process to buy a second session.",
      "defended_by": "The claim is held by the identity engine, not by the edge that minted it, so spending it on one process spends it for all of them.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "aDesktopClaimIsSpentOnceAcrossReplicas"
    },
    {
      "id": "desktop-claim-verifier-guessing",
      "what": "A claim code is held and verifiers are tried against it repeatedly, on any process that will answer.",
      "defended_by": "The code is taken out of the store before the verifier is checked, so a wrong guess spends it: one code, one attempt, deployment-wide.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "aDesktopClaimWithTheWrongVerifierIsRefusedAndSpent"
    },
    {
      "id": "replicated-edge-with-per-process-sessions",
      "what": "A deployment runs several edge processes with identity in process, so each holds its own session table and a revoked or elevated session is judged differently by each of them.",
      "defended_by": "synqt check refuses replicas > 1 without identity.provider_entity, naming the key that moves the session table to one authoritative entity.",
      "file": "tools/synqt/tests/test_check_replicas.py",
      "test": "test_a_replicated_edge_needs_a_provider_entity"
    },
    {
      "id": "replicated-edge-with-an-embedded-device-store",
      "what": "A device credential is enrolled through one replica and presented to another, which holds a different embedded store and cannot tell a real credential from a forged one because it has never seen either.",
      "defended_by": "synqt check refuses an embedded (sqlite/memory) identity.device.store under replicas > 1.",
      "file": "tools/synqt/tests/test_check_replicas.py",
      "test": "test_an_embedded_device_store_is_an_error"
    },
    {
      "id": "unauthenticated-bundle-fetch",
      "what": "A visitor with no session asks for a file of a bundle their scope does not map to, to read the QML of a view the route guard would only have refused to navigate to.",
      "defended_by": "The edge resolves the bundle from the caller's session scope before it looks any file up, so a path outside that bundle resolves to nothing. It answers 404 rather than 403: a private deployment does not confirm that an operator console exists.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "anAssetOfAnotherBundleIsNotFound"
    },
    {
      "id": "deep-link-bundle-leak",
      "what": "A visitor with no session follows a deep link into a privileged route, which the client route does not answer and the shell fallback does.",
      "defended_by": "shellOrNotFound serves the shell of the caller's own bundle, so a deep link with no session lands on the gate instead of on an application shell that would then fail to fetch its module.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "aDeepLinkWithNoSessionLandsOnTheGateShell"
    },
    {
      "id": "monitor-credential-capture",
      "what": "A session credential, a token or an Authorization header is written into the monitoring record, so anyone who can read the monitor can replay the session it belongs to.",
      "defended_by": "Every session event names the session by SessionManager::keyFor (half its SHA-256), never by its id, and the edge records an upgrade's decision and reason rather than the request that carried it. The tests render each recorded event back to JSON and assert the credential appears nowhere in it.",
      "file": "tests/monitor/tst_instrumentation.cpp",
      "test": "theSessionLifecycleIsRecordedByItsHandleAndNotItsCredential"
    },
    {
      "id": "monitor-event-flood",
      "what": "A caller drives an entity into recording faster than the monitor can take events, to exhaust its memory or block its event loop through the pipeline it cannot turn off.",
      "defended_by": "The ring is bounded and drops the oldest rather than growing or blocking, counting every drop so the gap stays visible, and each event's message and attributes are truncated at the pipeline rather than at the call site, so an oversized header cannot become an oversized record.",
      "file": "tests/monitor/tst_pipeline.cpp",
      "test": "aFullRingDropsAndCountsRatherThanGrowing"
    },
    {
      "id": "monitor-identity-copy",
      "what": "A contract asks for its call arguments to be recorded, and those arguments carry who the caller is, so the monitoring record becomes a second copy of the identity store: kept longer than a session, readable by people the identities are not about, and exported to whatever collector an operator points it at.",
      "defended_by": "Value capture is off unless a member writes `capture`, and `synqt check` refuses `capture` on a member whose arguments carry an identity field (directly or through a record) unless the project writes `monitoring.capture_identity: acknowledged`. Without the modifier the generated span records the shape of a call only: contract, member, caller kind and argument count.",
      "file": "tools/synqt/tests/test_check_monitoring.py",
      "test": "test_an_identity_reached_through_a_record_is_refused_too"
    },
    {
      "id": "monitor-spool-disk-exhaustion",
      "what": "A monitor stays unreachable, so every entity reporting to it keeps writing what it could not hand over, and the spool fills the disk of the entity it was supposed to be watching.",
      "defended_by": "The spool is bounded by `monitoring.spool_cap_bytes`. Past the cap the oldest batches are dropped and the newest kept, and the number dropped is published to the monitor when it returns, so the gap is visible rather than silent.",
      "file": "tests/monitor/tst_ingest.cpp",
      "test": "theSpoolIsBoundedAndTheOverflowIsReported"
    },
    {
      "id": "monitor-reachable-from-the-internet",
      "what": "The monitoring console is bound to a public interface, so every request the system has served, every refusal and the shape of every entity in it are one password away from anyone who finds the port.",
      "defended_by": "A monitor binds 127.0.0.1 by default, and `synqt check` refuses any other host unless the project writes `monitoring: {public: acknowledged}`. Reaching the console then means reaching the machine: a VPN, an SSH tunnel, or being on the host. The delivery gate applies either way, so even on a public interface the bundle is not addressable without an operator session.",
      "file": "tools/synqt/tests/test_monitoring.py",
      "test": "test_a_monitor_on_a_public_interface_has_to_say_so"
    },
    {
      "id": "monitor-record-reaches-the-application-client",
      "what": "The application's own client is listed as a consumer of a point the monitor owns, so every entity's record is delivered to whoever can sign in to the application, behind the application's scope vocabulary rather than the operator gate.",
      "defended_by": "`synqt check` refuses a client on the consumer list of any point a monitor owns unless that client is the console (`console: true`), which the monitor delivers itself behind the operator password gate. The framework's own console point is only ever given to console clients, and the ingest point never to a client at all.",
      "file": "tools/synqt/tests/test_monitoring.py",
      "test": "test_the_application_client_cannot_consume_what_the_monitor_owns"
    },
    {
      "id": "monitor-export-key-in-repository",
      "what": "The API key of the collector a monitor exports to is written into synqt.yaml, so a credential that grants write access to the operator's whole observability backend is committed alongside the code and readable by everyone with the repository.",
      "defended_by": "There is no configuration key to write it into. `export.otlp` names the endpoint and nothing else, and the generated main fills the request headers from OtlpExporter::headersFromEnvironment(), which reads the monitor entity's own environment. The same rule every other credential in SynQt follows.",
      "file": "tools/synqt/tests/test_monitor_export.py",
      "test": "test_the_collectors_api_key_is_read_from_the_environment_and_never_from_the_project"
    },
    {
      "id": "monitor-export-exhausts-the-monitor",
      "what": "The collector a monitor exports to stops answering, or the file it writes lines to is never rotated, so the exporter queues without a bound or grows without one and the monitor takes down the machine it was watching.",
      "defended_by": "An exporter drops rather than queues: past `max_in_flight` requests a batch is dropped and counted, and the JSONL file is capped and rotated with a bounded number of files. Exporting happens after the history is written, so a dead collector costs the monitor no record and no time, and `synqt check` warns about a jsonl file with no cap.",
      "file": "tests/monitor/tst_export.cpp",
      "test": "aCollectorThatIsDownCostsTheMonitorNothingAndLosesNoHistory"
    },
    {
      "id": "client-forged-monitoring-report",
      "what": "A browser reports fabricated events as if it were an entity, so the operations record shows activity that never happened, or shows it as coming from an entity the visitor is not.",
      "defended_by": "No client ever consumes the ingest point: appmodel.monitoring_connect_points lists every service and never a client, and a browser cannot reach the mesh at all. What a browser does reaches the record through the edge that served it, where it is a fact the edge observed rather than a claim the browser made.",
      "file": "tools/synqt/tests/test_monitoring.py",
      "test": "test_a_client_never_consumes_it"
    },
    {
      "id": "monitor-console-open-by-default",
      "what": "The monitoring console is reachable with no operator configured, so whoever can reach the monitor's port reads every request the system has served.",
      "defended_by": "OperatorStore fails closed: an empty store refuses every sign-in rather than allowing all, credentials are read from the monitor entity's own environment and never from synqt.yaml, and a credential derived with fewer than 600000 PBKDF2 iterations is refused at load rather than accepted with a warning.",
      "file": "tests/monitor/tst_operator.cpp",
      "test": "aMonitorWithNoOperatorsRefusesEverybody"
    },
    {
      "id": "monitor-console-bundle-fetch",
      "what": "A visitor with no operator session downloads the monitoring console bundle and reads it, or reaches its connect point directly, learning what the system is made of and what it has been doing.",
      "defended_by": "The console is delivered through `bundles:`, so an anonymous caller is handed a static sign-in page and the console bundle is not addressable to them at all (404, not 403). Its connect point is gated on `operator`, which only the monitor's own password gate grants, and the monitor listens on loopback by default. Proven in a real browser against a real monitor, not only as a string: tests/monitor-console fetches the console's files as an anonymous visitor and asserts 404 on each.",
      "file": "tests/monitor-console/verify/verify.mjs",
      "test": "theConsoleBundleIsNotAddressableToAnAnonymousCaller"
    },
    {
      "id": "second-client-session-field",
      "what": "A project with two clients (an application and a monitoring console) puts the forwarded-session field on the second client's link, giving a browser a field to put a session of its own choosing in.",
      "defended_by": "appmodel.forwards_session compares a point's consumers against every client entity, not the first one. A point only a browser consumes carries nothing beyond its contract, which is what leaves the one caller that could forge a session with nowhere to put it.",
      "file": "tools/synqt/tests/test_monitoring.py",
      "test": "test_a_second_client_still_has_no_session_field_to_forge"
    },
    {
      "id": "monitor-console-served-to-the-public",
      "what": "The operator console is mapped to a scope below `operator`, or the monitor is left with no bundle map at all, so the page that shows every request the system has handled and every refusal is delivered to whoever reaches the port. It builds, it runs, and nothing says so.",
      "defended_by": "`synqt check` reads the resolved bundle map of every browser-facing entity, the monitor included, and refuses a `console: true` client mapped to any scope but `operator`, on a monitor or on an application edge. It refuses a monitor whose default scope resolves to a client at all, which is what a monitor with no map falls back to: the project's first client, baked into the generated main as what that port serves.",
      "file": "tools/synqt/tests/test_check_monitoring.py",
      "test": "test_the_console_served_to_anonymous_is_refused"
    },
    {
      "id": "outbound-allowlist-escaped-by-a-redirect",
      "what": "An allowlisted third party answers a redirect to a host network.outbound never named. Qt follows it by default and carries the request's headers, which on this path are the endpoint's own credential headers, so the deployment's API key leaves and the entity reaches somewhere it may not.",
      "defended_by": "Http sends every call under UserVerifiedRedirectPolicy and puts each hop through the same allowlist the first one passed; a target it does not cover is never allowed and the reply is abandoned.",
      "file": "tests/m9-providers/tst_m9.cpp",
      "test": "aRedirectOutOfTheAllowlistIsRefused"
    },
    {
      "id": "id-token-with-no-expiry",
      "what": "A provider signs an ID token carrying no exp claim, so a copy of it opens a session for as long as the signing key lives. The same token with no sub signs every visitor in as one empty subject.",
      "defended_by": "JwksVerifier requires exp and sub rather than checking exp only when it is present.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "anIdTokenMissingARequiredClaimIsRefused"
    },
    {
      "id": "logout-csrf",
      "what": "Another site navigates a visitor to the logout route, which is a GET, and ends their session and the device credential that would have kept them signed in. SameSite=Lax is sent on a top-level navigation, and split_origin's cookie is sent on everything.",
      "defended_by": "The logout route refuses a request whose Sec-Fetch-Site says it came from another site. A non-browser caller sends none and is unaffected.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "logoutFromAnotherSiteIsRefused"
    },
    {
      "id": "operator-enumeration-by-clock",
      "what": "The console's sign-in answers one refusal to every failure, but returned before deriving when the operator name was unknown, so the clock said which names exist and a guesser could spend every attempt on one that does.",
      "defended_by": "OperatorStore::verify reads the whole list and derives once either way, against the first credential's parameters when the name is unknown.",
      "file": "tests/monitor/tst_operator.cpp",
      "test": "anUnknownOperatorCostsWhatAKnownOneCosts"
    },
    {
      "id": "sign-in-flood",
      "what": "Unauthenticated POSTs to the password gate, each one a guess and each one a PBKDF2 at the operator store's round count on the edge's own event loop: a brute force and a way to stop the edge answering anybody else at the same time.",
      "defended_by": "A per-address fixed window, spent before the credential is read, answering 429 with Retry-After.",
      "file": "tests/monitor/tst_instrumentation.cpp",
      "test": "aFloodOfSignInAttemptsIsRefusedBeforeThePasswordIsChecked"
    },
    {
      "id": "inbound-body-past-the-declared-limit",
      "what": "network.inbound.max_body_bytes was checked after QHttpServer had already read the body, so a caller could spend Qt's own 32 MiB default per connection whatever the topology declared.",
      "defended_by": "ApiServer sets the declared limit on QHttpServerConfiguration, so the transport refuses an oversized body before a route sees it.",
      "file": "tests/api-inbound/tst_apiinbound.cpp",
      "test": "aBodyPastTheLimitIsRefusedByTheTransportAndNotByTheHandler"
    },
    {
      "id": "provider-tokens-outlive-the-session",
      "what": "A session is revoked -- which is what a detected device-credential reuse does to every session that credential opened -- and the edge goes on holding that visitor's provider access and refresh tokens, live, for as long as the process runs.",
      "defended_by": "Both of the ways a session ends without anybody pressing anything are wired to the release: expiry and revocation. The rotation that a scope change reports as a removal is read and handed to followRotation instead, so an elevation does not look like an ending.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "revokingASessionReleasesItsProviderTokens"
    },
    {
      "id": "provider-tokens-orphaned-by-an-elevation",
      "what": "A sign-in elevates a scope, which rotates the session credential, and the provider tokens stay keyed on the credential that was replaced. The live session can no longer reach its tokens, and the unreachable entry is refreshed against the provider forever.",
      "defended_by": "The identity provider follows the rotation: the tokens and the device family move to the new credential, on the edge or on the auth entity, the same way the edge moves the socket and the session's Sources.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "elevatingASessionCarriesItsProviderTokens"
    },
    {
      "id": "operator-name-enumeration-by-clock",
      "what": "The monitor's sign-in gate answers one 'no' whatever was wrong, but answers it sooner for a name that does not exist, so a guesser learns which operators are real by timing the refusals. It leaked again once the store held operators with different round counts: an unknown name was worked against whichever credential was listed first.",
      "defended_by": "Every refusal costs one derivation, and for an unknown name it is the most expensive credential in the store, so no unknown name is ever answered faster than a known one.",
      "file": "tests/monitor/tst_operator.cpp",
      "test": "anUnknownOperatorCostsWhatTheDearestKnownOneCosts"
    },
    {
      "id": "rate-limit-reset-by-flooding-the-window-table",
      "what": "Every gate that rations by client address keeps a table keyed by address, and each one emptied that table when it grew too large. So a guesser who can present addresses -- an IPv6 /64, or a forwarding header on a deployment that trusts one -- fills it with entries they will never use again, the table is emptied, and the count against the address they are guessing from is back to zero. The ceiling was the reset.",
      "defended_by": "The table drops only the windows that have run out, which are evidence of nothing. When that frees nothing the gate refuses for the rest of the window rather than forgetting what it knows.",
      "file": "tests/m5-webedge/tst_ratewindow.cpp",
      "test": "aFloodOfLiveAddressesDoesNotClearTheGuessersOwnCount"
    },
    {
      "id": "api-rate-limit-escaped-by-a-forged-forwarding-header",
      "what": "An entity's inbound API surface rations requests per address. A caller who can decide which address that is has no limit at all, not a generous one: a different X-Forwarded-For on every request buys a fresh budget every time.",
      "defended_by": "The header is read only from a peer the entity named in network.inbound.trusted_proxies, and within it only the rightmost entry that is not itself a listed hop, since a balancer appends what it saw rather than replacing what the client sent. A surface that names nobody counts the peer that connected and nothing else.",
      "file": "tests/api-inbound/tst_apiinbound.cpp",
      "test": "aForgedForwardedHeaderDoesNotBuyAFreshBudget"
    },
    {
      "id": "palette-import-hidden-by-a-line-separator",
      "what": "A page the edge delivers is code, and the palette is what bounds the modules it may import. QML's lexer ends a line at U+2028 and U+2029 as well as at a line feed and a carriage return, so a page can close a line comment with one and put an import after it. A scan that ends a comment only at the two terminators people type reads the import as part of the comment, never looks at it, and accepts a page that imports a module the palette never allowed.",
      "defended_by": "The palette reads the page with the lexer's own notion of where a line ends: all four terminators close a line comment, close an unterminated string literal, and separate two statements.",
      "file": "tests/remote-pages/tst_remotepage.cpp",
      "test": "paletteRejectsAnImportHiddenByALineSeparatorInAComment"
    },
    {
      "id": "an-edge-listening-with-no-key",
      "what": "An edge is configured to terminate TLS and given a private key it cannot read: an elliptic-curve key where only RSA was asked for, a path that is not a key, a file it cannot open. QSslKey answers a null key and nothing about that looks like an error, so the edge listens on the public port and fails every handshake. To a visitor the site is down; to an operator there is nothing in the log naming the file, and the temptation is to reach for the plaintext port.",
      "defended_by": "The key is read by trying every algorithm rather than one, so an ordinary EC key works; and an edge told to terminate TLS and unable to refuses to start, naming the two files.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "theEdgeRefusesToStartWithAKeyItCannotRead"
    },
    {
      "id": "an-edge-listening-with-a-key-its-backend-cannot-present",
      "what": "The same dead port by another road. The key is read, so nothing about it looks wrong, but the TLS backend this build runs on cannot hand it to the platform: a backend with no key API of its own converts the certificate and the key into a PKCS#12 blob, and Qt writes an algorithm identifier for RSA and DSA and for nothing else, so an elliptic-curve key arrives malformed, the platform import fails, and the socket is left with no identity. The edge listens on the public port and every handshake fails, which is the state the file check was added to prevent, reached without ever failing that check.",
      "defended_by": "Reading the key is not the last question asked of it. A web edge, an inbound API surface and a mesh owner each ask whether the active TLS backend can present the key they just read, and refuse to start naming the file and the backend when it cannot.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "theEdgeTerminatesTlsWithAnEllipticCurveKey"
    },
    {
      "id": "a-refresh-sweep-that-never-stops",
      "what": "An identity provider answers a refresh without naming how long the new token lasts, which RFC 6749 section 5.1 permits (`expires_in` is RECOMMENDED, not REQUIRED). The edge kept the expiry the refresh replaced, so the entry stayed inside the sweep's margin: the next pass refreshed it again, and every pass after that, spending a rotating refresh token against the provider once per interval for the life of every such session. The load is on a third party and grows with the number of signed-in users, and a provider that rate-limits or that invalidates a rotated pair sees it as abuse from the deployment.",
      "defended_by": "A refresh writes the lifetime the provider named or none at all, never the one that expired, which is what the code exchange already did; the sweep skips an entry whose expiry nobody named.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "aRefreshThatNamesNoLifetimeIsNotSweptAgain"
    },
    {
      "id": "a-plaintext-feed-of-the-security-record",
      "what": "A monitor is pointed at an OpenTelemetry collector on another host over plaintext http. Every batch is the whole record of what the system did: which member each caller called, which upgrades were refused and for what reason, which mesh peer connected under which certificate subject. The request carrying it also carries the collector's API key. Anyone on the path reads the security record of the deployment and holds the credential for the store keeping it, and can forge records into that store.",
      "defended_by": "The exporter refuses the endpoint at startup and says so once, counting every event it is then handed as dropped so the accounting never implies a collector is being fed; https is accepted anywhere and plaintext only to this machine, where the traffic reaches no network. `synqt check` reports the same rule, and refuses it outright for a release build.",
      "file": "tests/monitor/tst_export.cpp",
      "test": "aPlaintextCollectorOnAnotherHostIsRefusedAndNotQuietlyFed"
    },
    {
      "id": "the-caller-before-this-one",
      "what": "A shared entity answers everybody from one Source, and the Caller in that Source's QML is re-pointed at whoever is calling before each slot runs. Its properties were declared CONSTANT, so QML never re-read them: a binding such as `text: Caller.identity.name`, or any gate written against `Caller.scope`, kept the answer for the first caller of the process and served it to every caller after. What shows on one visitor's page, and what a member is published against, is then another visitor's identity and another visitor's scope.",
      "defended_by": "None of the nine properties is CONSTANT; all of them notify on one signal, raised when the caller is adopted and when an elevation rotates the session, and only when something an accessor reads actually moved.",
      "file": "tests/m7-caller/tst_memberscope.cpp",
      "test": "aBindingOnTheCallerFollowsWhoIsCallingNow"
    },
    {
      "id": "a-credential-written-into-the-record",
      "what": "A credential reaches a trace call under its own name, and is then held for as long as the record is: an application chasing a refusal writes `Log.warn(\"refused\", { authorization: header })`, or a call site added later records a cookie beside the reason it was rejected. Whoever can read the monitoring console, the JSONL file, or the collector the events are exported to now holds a live bearer token or session cookie, without having attacked anything.",
      "defended_by": "Every event passes through Tracer::record, past anything QML can reach, and an attribute whose name names a credential is recorded as `[redacted]` with its name kept. It is the backstop under the call-site rule that the framework records handles rather than secrets, not a substitute for it: names are read, values never are.",
      "file": "tests/monitor/tst_pipeline.cpp",
      "test": "aValueUnderASecretNameIsNeverWhatIsRecorded"
    },
    {
      "id": "two-nonces-in-one-authorization-request",
      "what": "An OpenID Connect authorization request carries the `nonce` parameter twice, with two different values: Qt puts one in whenever the scope contains `openid`, and the framework added a second beside it. RFC 6749 section 3.1 forbids a repeated parameter, so a provider that enforces it answers invalid_request and nobody signs in at all; one that does not enforce it echoes whichever value it read first into the ID token, which decides by coin toss whether the nonce check the edge then runs can pass. A nonce that may or may not match is not a replay defence, and a login that fails on the strict providers is a login that only ever worked against a lenient stub.",
      "defended_by": "The framework hands Qt its own random token through setNonce and sets NonceMode::Enabled, rather than inserting a parameter beside the one Qt generates, so exactly one nonce leaves the edge and it is the one the returned ID token is checked against.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "anAuthorizationRequestCarriesOneNonce"
    },
    {
      "id": "a-shipped-edge-carries-the-development-sign-in",
      "what": "The stub identity provider signs anybody in as a preconfigured person with no password, and it was compiled into every SynQtEdge, held back only by runtime checks: a --dev flag, an allowDevStub field, and a tag type on its constructor. Three runtime gates are three chances to be wrong about a binary that still contains the thing. A bug in any check, an argument someone passes, or a reader of the artifact strings reaches a login that mints any identity.",
      "defended_by": "The sources are named in src/edge/CMakeLists.txt only under SYNQT_DEV_TOOLS, which synqt dev sets and synqt build never does; the header refuses to be included by a build that did not set it; and the generated edge main does not emit the construction for a release profile. The runtime gates all remain, but they are now the third layer rather than the only one, and what a release artifact contains no longer depends on any of them.",
      "file": "tests/dev-exclusion/run-dev-exclusion.sh",
      "test": "aReleaseEdgeDoesNotContainTheDevelopmentSignIn"
    },
    {
      "id": "a-mapping-hook-invents-a-scope",
      "what": "The project's scopeFor() returns a scope name that is not one of the scopes in scopes.order, because the hook was written against a different vocabulary or the name was mistyped. The edge took the string and used it, so the session held a scope no connect point gate and no route guard could satisfy: the visitor signed in successfully and could reach nothing, and no layer anywhere reported why. The same code path answered \"user\" when the hook failed outright, so a renamed or uncompilable hook handed every visitor an authenticated scope instead.",
      "defended_by": "The hook returns a member of the generated Scope.Value enum rather than a string, and IdentityProvider::mapScope resolves that member as an index into the declared vocabulary the edge was given: an answer outside the range names no scope and fails the login closed, with the reason logged, and there is no fallback left for a hook that does not answer. synqt check refuses a member scopes.order does not declare before the build, and refuses a project that serves a sign-in while declaring no scopes or naming no hook.",
      "file": "tests/m8-auth/tst_m8.cpp",
      "test": "aHookAnswerOutsideTheVocabularyFailsTheLoginClosed"
    },
    {
      "id": "a-development-edge-serves-the-scope-picker-to-anybody",
      "what": "The development scope picker mints a session at any scope the project declares, with no credential of any kind: that is what it is for, and it is why reaching it from anything but a developer's own `synqt dev` would be a complete authorization bypass. A route that exists and refuses is still a route, and it answers differently from a path the edge does not serve, so a caller guessing /synqt/dev/identity learns whether the edge has one.",
      "defended_by": "Three layers, none of which is the only one. The picker's sources are named in src/edge/CMakeLists.txt only under SYNQT_DEV_TOOLS, so a release SynQtEdge does not contain the class (proven by adding IdentityPicker to tests/dev-exclusion's DEV_SYMBOLS); the header refuses to be included by a build that did not set it; and the routes are registered only when `synqt dev --identity-picker` set WebEdgeConfig::identityPicker, which nothing built and nothing served passes. Inside the POST, the chosen scope is bounds-checked against the declared vocabulary, so an edited form cannot name a scope the project never declared.",
      "file": "tests/m5-webedge/tst_m5.cpp",
      "test": "aDevEdgeWithoutTheFlagHasNoPickerRoute"
    }
  ]
}
