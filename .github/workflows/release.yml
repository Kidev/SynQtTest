# SPDX-FileCopyrightText: 2026 Alexandre 'kidev' Poumaroux
# SPDX-License-Identifier: Apache-2.0

# Cut a SynQt release: freeze the synqt CLI into one binary per operating system and
# architecture, publish a GitHub release with those assets, and upload the distribution to
# PyPI. The asset names are what get.synqt.org's install.sh downloads. The version is the
# latest v* tag bumped by the chosen component, plus an optional pre-release suffix; a dry
# run builds and smoke-tests everything and publishes nothing.
#
# Linux is frozen inside the manylinux_2_28 container, so the binary's glibc floor is 2.28
# whatever runner image is current; macOS and Windows take their floor from the setup-python
# interpreter. Every row smoke-tests its own binary where it was built.

name: "[RELEASE] Publish a release of the synqt CLI"

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Which version component to bump from the latest v* tag"
        required: true
        type: choice
        default: patch
        options:
          - patch
          - minor
          - major
      suffix:
        description: "Optional pre-release suffix appended verbatim, e.g. -alpha or -rc.1 (leave empty for a stable release)"
        required: false
        type: string
        default: ""
      dry_run:
        description: "Dry run: build and smoke-test every artifact but create no tag and publish no release"
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  # get.synqt.org serves the installer at both / and /install.sh, so index.html has to be a
  # byte for byte copy of install.sh.
  installer:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - name: index.html must be identical to install.sh
        run: |
          if ! diff -u deploy/get.synqt.org/install.sh deploy/get.synqt.org/index.html; then
            echo "::error::deploy/get.synqt.org/index.html must be a byte for byte copy of install.sh (run: cp deploy/get.synqt.org/install.sh deploy/get.synqt.org/index.html)"
            exit 1
          fi

  # Inputs are passed through the environment, never interpolated into a script, so a crafted
  # suffix cannot inject shell.
  version:
    runs-on: ubuntu-24.04
    outputs:
      version: ${{ steps.compute.outputs.version }}
      tag: ${{ steps.compute.outputs.tag }}
      prerelease: ${{ steps.compute.outputs.prerelease }}
      pypi: ${{ steps.pep440.outputs.pypi }}
      pypi_version: ${{ steps.pep440.outputs.pypi_version }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Compute the next version
        id: compute
        env:
          BUMP: ${{ inputs.bump }}
          SUFFIX: ${{ inputs.suffix }}
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -eu
          latest="$(git tag --list 'v[0-9]*' --sort=-version:refname | head -n1)"
          first="false"
          if [ -z "$latest" ]; then
            latest="v0.0.0"
            first="true"
          fi
          core="${latest#v}"
          core="${core%%-*}"
          major="${core%%.*}"
          rest="${core#*.}"
          minor="${rest%%.*}"
          patch="${rest#*.}"
          case "$BUMP" in
            major) major=$((major + 1)); minor=0; patch=0 ;;
            minor) minor=$((minor + 1)); patch=0 ;;
            patch) patch=$((patch + 1)) ;;
            *) echo "::error::unknown bump: $BUMP"; exit 1 ;;
          esac
          version="${major}.${minor}.${patch}${SUFFIX}"
          if [ -n "$SUFFIX" ]; then
            prerelease="true"
          else
            prerelease="false"
          fi
          if [ "$first" = "true" ]; then
            echo "No existing v* tag: first release -> new version: $version (prerelease=$prerelease)"
          else
            echo "Latest tag: $latest -> new version: $version (prerelease=$prerelease)"
          fi
          if [ "$DRY_RUN" = "true" ]; then
            echo "::notice::Dry run: v$version will be built and smoke-tested but NOT tagged or published"
          fi
          {
            echo "version=$version"
            echo "tag=v$version"
            echo "prerelease=$prerelease"
          } >> "$GITHUB_OUTPUT"

      # A suffix PEP 440 cannot express (`-nightly`) skips PyPI and still releases on GitHub.
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Decide whether this version is publishable to PyPI
        id: pep440
        env:
          SYNQT_VERSION: ${{ steps.compute.outputs.version }}
        run: |
          set -eu
          python -m pip install --quiet --disable-pip-version-check packaging
          python - <<'PY'
          import os

          from packaging.version import InvalidVersion, Version

          raw = os.environ["SYNQT_VERSION"]
          try:
              normalized = str(Version(raw))
          except InvalidVersion:
              publishable, normalized = "false", ""
              print(f"::notice::{raw} is not a PEP 440 version, so this release publishes "
                    "the GitHub assets only and skips PyPI.")
          else:
              publishable = "true"
              print(f"{raw} is a PEP 440 version (normalizes to {normalized}); PyPI is in.")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as handle:
              handle.write(f"pypi={publishable}\npypi_version={normalized}\n")
          PY

  # The container is run directly rather than as the job `container:`, so the Actions run on
  # the host's glibc and only the freeze and smoke test happen inside the image.
  build-linux:
    needs: version
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            arch_tag: x86_64
            image: quay.io/pypa/manylinux_2_28_x86_64
          - runner: ubuntu-24.04-arm
            arch_tag: arm64
            image: quay.io/pypa/manylinux_2_28_aarch64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v7

      - name: Stamp the CLI version
        shell: bash
        # Written by repr(), never spliced into a literal: the value carries the free-form
        # suffix input.
        env:
          SYNQT_VERSION: ${{ needs.version.outputs.version }}
        run: |
          python3 - <<'PY'
          import os

          version = os.environ["SYNQT_VERSION"]
          lines = [
              "# SPDX-FileCopyrightText: 2026 Alexandre 'kidev' Poumaroux",
              "# SPDX-License-Identifier: Apache-2.0",
              "",
              '"""Stamped by the release workflow. See tools/synqt/synqt/_version.py."""',
              "",
              "__version__ = " + repr(version),
              "",
          ]
          with open("tools/synqt/synqt/_version.py", "w", encoding="utf-8") as handle:
              handle.write("\n".join(lines))
          PY

      - name: Freeze and smoke-test inside manylinux_2_28 (glibc 2.28 floor)
        env:
          IMAGE: ${{ matrix.image }}
        # --collect-submodules pulls the command modules cli.py imports lazily by name, and
        # --collect-data the package's data files. The interpreter is the dnf python by
        # absolute path: the /opt/python ones on PATH are static builds PyInstaller refuses.
        run: |
          set -eu
          docker run --rm -v "$PWD:/io" -w /io "$IMAGE" bash -euo pipefail -c '
            dnf install -y python3.12 python3.12-pip || dnf install -y python3.11 python3.11-pip
            if [ -x /usr/bin/python3.12 ]; then py=/usr/bin/python3.12; else py=/usr/bin/python3.11; fi
            "$py" -m pip install --root-user-action=ignore --upgrade pip
            "$py" -m pip install --root-user-action=ignore ./tools/synqt pyinstaller
            printf "import sys\nfrom synqt.cli import main\nif __name__ == \"__main__\":\n    sys.exit(main())\n" > synqt_entry.py
            "$py" -m PyInstaller --onefile --name synqt \
              --collect-submodules synqt --collect-data synqt synqt_entry.py
            ./dist/synqt --help
            tmp="$(mktemp -d)"
            # A one-file binary unpacks into a directory it deletes on exit, which `synqt new`
            # refuses to bake into a project; point it at the checkout.
            SYNQT_ROOT=/io ./dist/synqt new smoketest --parent-dir "$tmp"
            test -f "$tmp/smoketest/synqt.yaml"
            ./dist/synqt providers
          '

      - name: Package the asset
        id: package
        run: |
          set -eu
          asset="synqt-linux-${{ matrix.arch_tag }}.tar.gz"
          mkdir -p out
          tar -C dist -czf "out/$asset" synqt
          echo "asset=$asset" >> "$GITHUB_OUTPUT"

      # Retried once: Actions has no step-level retry, and a failed upload discards a
      # finished binary. The retry overwrites what a half-finished attempt registered.
      - uses: actions/upload-artifact@v7
        id: upload
        continue-on-error: true
        with:
          name: ${{ steps.package.outputs.asset }}
          path: out/${{ steps.package.outputs.asset }}
          if-no-files-found: error

      - name: Retry the upload if the artifact endpoint blipped
        if: steps.upload.outcome == 'failure'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.package.outputs.asset }}
          path: out/${{ steps.package.outputs.asset }}
          if-no-files-found: error
          overwrite: true

  build-native:
    needs: version
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15-intel
            os_tag: macos
            arch_tag: x86_64
            ext: tar.gz
          - os: macos-15
            os_tag: macos
            arch_tag: arm64
            ext: tar.gz
          - os: windows-2025
            os_tag: windows
            arch_tag: x86_64
            ext: zip
          - os: windows-11-arm
            os_tag: windows
            arch_tag: arm64
            ext: zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Stamp the CLI version
        shell: bash
        # Written by repr(), never spliced into a literal: the value carries the free-form
        # suffix input.
        env:
          SYNQT_VERSION: ${{ needs.version.outputs.version }}
        run: |
          python3 - <<'PY'
          import os

          version = os.environ["SYNQT_VERSION"]
          lines = [
              "# SPDX-FileCopyrightText: 2026 Alexandre 'kidev' Poumaroux",
              "# SPDX-License-Identifier: Apache-2.0",
              "",
              '"""Stamped by the release workflow. See tools/synqt/synqt/_version.py."""',
              "",
              "__version__ = " + repr(version),
              "",
          ]
          with open("tools/synqt/synqt/_version.py", "w", encoding="utf-8") as handle:
              handle.write("\n".join(lines))
          PY

      - name: Install the CLI and PyInstaller
        run: python -m pip install --upgrade pip && python -m pip install ./tools/synqt pyinstaller

      - name: Freeze synqt into a single binary
        shell: bash
        # --collect-submodules pulls the command modules cli.py imports lazily by name, and
        # --collect-data the package's data files.
        run: |
          set -eu
          printf 'import sys\nfrom synqt.cli import main\nif __name__ == "__main__":\n    sys.exit(main())\n' > synqt_entry.py
          pyinstaller --onefile --name synqt --collect-submodules synqt --collect-data synqt synqt_entry.py

      - name: Smoke-test the frozen binary
        shell: bash
        run: |
          set -eu
          if [ "${{ runner.os }}" = "Windows" ]; then
            bin=./dist/synqt.exe
          else
            bin=./dist/synqt
          fi
          "$bin" --help
          # A relative directory and a native GITHUB_WORKSPACE path: under Git bash on Windows
          # an MSYS path is not what the native .exe resolves.
          mkdir -p smoke-out
          SYNQT_ROOT="$GITHUB_WORKSPACE" "$bin" new smoketest --parent-dir smoke-out
          test -f smoke-out/smoketest/synqt.yaml
          "$bin" providers

      - name: Package the asset
        id: package
        shell: bash
        run: |
          set -eu
          asset="synqt-${{ matrix.os_tag }}-${{ matrix.arch_tag }}.${{ matrix.ext }}"
          mkdir -p out
          if [ "${{ runner.os }}" = "Windows" ]; then
            powershell -NoProfile -Command "Compress-Archive -Path dist/synqt.exe -DestinationPath out/$asset -Force"
          else
            tar -C dist -czf "out/$asset" synqt
          fi
          echo "asset=$asset" >> "$GITHUB_OUTPUT"

      # Retried once, as in build-linux.
      - uses: actions/upload-artifact@v7
        id: upload
        continue-on-error: true
        with:
          name: ${{ steps.package.outputs.asset }}
          path: out/${{ steps.package.outputs.asset }}
          if-no-files-found: error

      - name: Retry the upload if the artifact endpoint blipped
        if: steps.upload.outcome == 'failure'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.package.outputs.asset }}
          path: out/${{ steps.package.outputs.asset }}
          if-no-files-found: error
          overwrite: true

  # The same CLI as a Python distribution. Built here and uploaded by a separate job, so only
  # the upload job holds the OIDC token.
  build-pypi:
    needs: version
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Stamp the CLI version
        # Written by repr(), never spliced into a literal; pyproject.toml reads the version
        # out of this file.
        env:
          SYNQT_VERSION: ${{ needs.version.outputs.version }}
        run: |
          python3 - <<'PY'
          import os

          version = os.environ["SYNQT_VERSION"]
          lines = [
              "# SPDX-FileCopyrightText: 2026 Alexandre 'kidev' Poumaroux",
              "# SPDX-License-Identifier: Apache-2.0",
              "",
              '"""Stamped by the release workflow. See tools/synqt/synqt/_version.py."""',
              "",
              "__version__ = " + repr(version),
              "",
          ]
          with open("tools/synqt/synqt/_version.py", "w", encoding="utf-8") as handle:
              handle.write("\n".join(lines))
          PY

      - name: Build the sdist and the wheel
        # From the checkout root: tools/synqt/_build_backend.py vendors src/ and cmake/ into
        # the package.
        run: |
          set -eu
          python -m pip install --upgrade pip
          python -m pip install build twine
          python -m build --outdir dist tools/synqt

      - name: Check the distributions and confirm they carry the framework
        run: |
          set -eu
          python -m twine check dist/*
          python - <<'PY'
          import glob
          import sys
          import zipfile

          wheel = glob.glob("dist/*.whl")[0]
          names = zipfile.ZipFile(wheel).namelist()
          for required in ("synqt/framework/cmake/SynQtContracts.cmake",
                           "synqt/framework/examples/stall/synqt.yaml",
                           "synqt/assets/favicon.svg",
                           "synqt/assets/design/index.html"):
              if required not in names:
                  sys.exit(f"{wheel} is missing {required}")
          print(f"{wheel}: {len(names)} files, framework and assets present")
          PY

      # Retried once, as in build-linux. The release job attaches these to the GitHub release too.
      - uses: actions/upload-artifact@v7
        id: upload
        continue-on-error: true
        with:
          name: pypi-distributions
          path: dist/
          if-no-files-found: error

      - name: Retry the upload if the artifact endpoint blipped
        if: steps.upload.outcome == 'failure'
        uses: actions/upload-artifact@v7
        with:
          name: pypi-distributions
          path: dist/
          if-no-files-found: error
          overwrite: true

  # The only job a dry run skips. The tag and the release are created with the app token,
  # under SynQt-Operations.
  release:
    needs: [installer, version, build-linux, build-native, build-pypi]
    if: ${{ !inputs.dry_run }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Mint a token for the SynQt-Operations app
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ secrets.SYNQT_CLIENT_ID }}
          private-key: ${{ secrets.SYNQT_PRIVATE_KEY }}
          permission-contents: write

      - uses: actions/checkout@v7

      - name: Download all build assets
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: List the assets to publish
        run: ls -la dist

      - name: Publish the release and tag the build commit
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ steps.app-token.outputs.token }}
          tag_name: ${{ needs.version.outputs.tag }}
          name: SynQt ${{ needs.version.outputs.tag }}
          target_commitish: ${{ github.sha }}
          prerelease: ${{ needs.version.outputs.prerelease }}
          generate_release_notes: true
          files: dist/*

  # Trusted publishing (OIDC): the publisher registered on PyPI (docs/development.md,
  # "Publishing to PyPI") is matched on this workflow's file name, so renaming the file breaks
  # it. Last, because PyPI never lets a version be re-uploaded.
  publish-pypi:
    needs: [version, build-pypi, release]
    if: ${{ !inputs.dry_run && needs.version.outputs.pypi == 'true' }}
    runs-on: ubuntu-24.04
    environment:
      name: pypi
      url: https://pypi.org/p/synqt
    permissions:
      id-token: write
    steps:
      # A third guard beside the job's `if` and `needs: release`: an upload cannot be undone.
      - name: Refuse to publish on a dry run
        env:
          DRY_RUN: ${{ inputs.dry_run }}
          PYPI_VERSION: ${{ needs.version.outputs.pypi_version }}
        run: |
          set -eu
          if [ "$DRY_RUN" = "true" ]; then
            echo "::error::publish-pypi reached on a dry run; refusing to upload"
            exit 1
          fi
          echo "not a dry run: publishing $PYPI_VERSION to PyPI"

      - name: Download the distributions
        uses: actions/download-artifact@v8
        with:
          name: pypi-distributions
          path: dist

      - name: Publish to PyPI
        # No username or password is what selects the trusted publishing flow.
        uses: pypa/gh-action-pypi-publish@release/v1
